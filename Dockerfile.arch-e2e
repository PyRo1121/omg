# Dockerfile for E2E testing on Arch Linux
FROM archlinux:latest@sha256:b860afd5823683f7ea389ba5f00d812f4fe55f6f286dea329d2abeefa535e309

# Update system and install base dependencies
RUN pacman -Syu --noconfirm && \
    pacman -S --noconfirm \
        base-devel \
        git \
        sudo \
        vim \
        which \
        && \
    pacman -Scc --noconfirm

# Create test user with sudo access
RUN useradd -m -G wheel -s /bin/bash testuser && \
    echo "%wheel ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers

# Set up working directory
WORKDIR /opt/omg

# Copy OMG binary
COPY omg-binary /usr/local/bin/omg
RUN chmod +x /usr/local/bin/omg

# Verify installation
RUN omg --version

# Switch to test user for tests
USER testuser
WORKDIR /home/testuser

# Default command
CMD ["/bin/bash"]
