# OMG Documentation Deployment
# This workflow is DISABLED until GitHub Actions billing is enabled.
# Uncomment the 'on:' section to enable automatic deployments.

name: Deploy Docs

# UNCOMMENT THESE LINES TO ENABLE:
# on:
#   push:
#     branches: [main]
#     paths:
#       - 'docs/**'
#       - 'docs-site/**'
#   workflow_dispatch:

# Keep this for now to prevent accidental runs
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "deploy" to confirm'
        required: true

env:
  NODE_VERSION: '20'

jobs:
  build:
    name: Build Documentation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: docs-site/package-lock.json

      - name: Install dependencies
        working-directory: docs-site
        run: npm ci

      - name: Build documentation
        working-directory: docs-site
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: docs-build
          path: docs-site/build
          retention-days: 1

  deploy-cloudflare:
    name: Deploy to Cloudflare Pages
    needs: build
    runs-on: ubuntu-latest
    # Only deploy on main branch
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: docs-build
          path: docs-site/build

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy docs-site/build --project-name=omg-docs

  # Alternative: Deploy to GitHub Pages
  # deploy-github-pages:
  #   name: Deploy to GitHub Pages
  #   needs: build
  #   runs-on: ubuntu-latest
  #   permissions:
  #     pages: write
  #     id-token: write
  #   environment:
  #     name: github-pages
  #     url: ${{ steps.deployment.outputs.page_url }}
  #   steps:
  #     - name: Download build artifact
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: docs-build
  #         path: docs-site/build
  #
  #     - name: Setup Pages
  #       uses: actions/configure-pages@v4
  #
  #     - name: Upload to GitHub Pages
  #       uses: actions/upload-pages-artifact@v3
  #       with:
  #         path: docs-site/build
  #
  #     - name: Deploy to GitHub Pages
  #       id: deployment
  #       uses: actions/deploy-pages@v4
